# Xero app credentials — create an app at https://developer.xero.com/app/manage
# Redirect URI registered on the app must exactly match REDIRECT_URI below.
XERO_CLIENT_ID=
XERO_CLIENT_SECRET=
REDIRECT_URI=http://localhost:3000/callback

# Local server port
PORT=3000
